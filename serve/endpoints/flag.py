import tempfile

# Declare FlagResponseSchema using `pydantic`


def flag(data):
    # Validate flag data

    # Create temp file
    with tempfile.NamedTemporaryFile(mode="wb") as temp:
        ...
        # Create an s3 client

        # Dump object to s3 bucket

        # Declare flag response
